---
version: 2.0.0
title: server_dispatch_gleam
file: ./test/lando/router_snapshot_test.gleam
test_name: server_dispatch_snapshot_test
---
// Generated by lando — do not edit.

import gleam/dynamic
import lando_runtime/effect
import lando_runtime/wire
import server_context.{type ServerContext}


import pages/home_ as pages_home_
import pages/about as pages_about
import pages/products/new as pages_products_new
import pages/products/id_ as pages_products_id_

/// Handle an incoming WebSocket message.
/// Dispatches to the correct page's server_update.
/// The model and return types are dynamic — type safety is
/// enforced by the codegen (both sides built from same source).
pub fn handle_message(
  model: dynamic.Dynamic,
  page: String,
  msg: dynamic.Dynamic,
  server_context: ServerContext,
) -> #(dynamic.Dynamic, effect.Effect(dynamic.Dynamic)) {
  case page {
    "Home" -> {
      let page_model = wire.coerce(model)
      let page_msg = wire.coerce(msg)
      let #(new_model, effects) = pages_home_.server_update(page_model, page_msg, server_context)
      #(wire.coerce(new_model), wire.coerce(effects))
    }
    "About" -> {
      let page_model = wire.coerce(model)
      let page_msg = wire.coerce(msg)
      let #(new_model, effects) = pages_about.server_update(page_model, page_msg, server_context)
      #(wire.coerce(new_model), wire.coerce(effects))
    }
    "ProductsNew" -> {
      let page_model = wire.coerce(model)
      let page_msg = wire.coerce(msg)
      let #(new_model, effects) = pages_products_new.server_update(page_model, page_msg, server_context)
      #(wire.coerce(new_model), wire.coerce(effects))
    }
    "ProductsId" -> {
      let page_model = wire.coerce(model)
      let page_msg = wire.coerce(msg)
      let #(new_model, effects) = pages_products_id_.server_update(page_model, page_msg, server_context)
      #(wire.coerce(new_model), wire.coerce(effects))
    }
    _ -> #(model, effect.none())
  }
}

/// Get the initial server model for a page.
/// Returns #(Nil, none) for pages without a server_init function.
pub fn init_server_model(
  page: String,
  server_context: ServerContext,
  params: dynamic.Dynamic,
) -> #(dynamic.Dynamic, effect.Effect(dynamic.Dynamic)) {
  case page {
    "Home" -> {
      let #(model, effects) = pages_home_.server_init(server_context)
      #(wire.coerce(model), wire.coerce(effects))
    }
    "About" -> {
      let #(model, effects) = pages_about.server_init(server_context)
      #(wire.coerce(model), wire.coerce(effects))
    }
    "ProductsNew" -> {
      let #(model, effects) = pages_products_new.server_init(server_context)
      #(wire.coerce(model), wire.coerce(effects))
    }
    "ProductsId" -> {
      let id = wire.coerce(wire.tuple_element(params, 0))
      let #(model, effects) = pages_products_id_.server_init(server_context, id)
      #(wire.coerce(model), wire.coerce(effects))
    }
    _ -> #(dynamic.nil(), effect.none())
  }
}

