// Generated by lando — do not edit.
//
// Typed decoders for ETF-serialized custom types.
// Each function converts raw ETF values (atoms as strings,
// tuples as arrays) into proper Gleam constructor instances.

import { setResultCtors, setOptionCtors, setListCtors, setDictFromList } from "./decoders_prelude.mjs";

import { decode_int, decode_float, decode_string, decode_bool, decode_bit_array, decode_nil, decode_list_of, decode_option_of, decode_result_of, decode_dict_of, decode_tuple_of, DecodeError } from "./decoders_prelude.mjs";
import { Ok, Error as ResultError, Empty, NonEmpty } from "../gleam_stdlib/gleam.mjs";
import { Some, None } from "../gleam_stdlib/gleam/option.mjs";
import { from_list as dictFromList } from "../gleam_stdlib/gleam/dict.mjs";
import { ArticleData, FavoriteUpdated, FavoriteCountUpdated, FollowUpdated, CommentAdded, CommentRemoved, ArticleDeleted, ArticleError, ArticleLoaded, ArticleUpdated, EditorErrors, ArticlePublished, HomeData, ArticleListUpdated, Authenticated, AuthError, ProfileData, ProfileArticles, Registered, RegisterError, SettingsLoaded, SettingsUpdated, SettingsError, LoggedOut } from "./types.mjs";

setResultCtors(Ok, ResultError);
setOptionCtors(Some, None);
setListCtors(Empty, NonEmpty);
setDictFromList(dictFromList);


export function decode_push_article_slug(term) {
  const tag = Array.isArray(term) ? term[0] : term;
  switch (tag) {
    case "article_data":
      return new ArticleData(decode_Article_article(term[1]), decode_list_of((t0) => decode_Comment_comment(t0), term[2]), decode_bool(term[3]), decode_bool(term[4]), decode_int(term[5]));
    case "favorite_updated":
      return new FavoriteUpdated(decode_int(term[1]), decode_bool(term[2]));
    case "favorite_count_updated":
      return new FavoriteCountUpdated(decode_int(term[1]));
    case "follow_updated":
      return new FollowUpdated(decode_bool(term[1]));
    case "comment_added":
      return new CommentAdded(decode_Comment_comment(term[1]));
    case "comment_removed":
      return new CommentRemoved(decode_int(term[1]));
    case "article_deleted":
      return new ArticleDeleted();
    case "article_error":
      return new ArticleError(decode_string(term[1]));
    default:
      throw new DecodeError("unknown push variant: " + String(tag));
  }
}


export function decode_push_editor_slug(term) {
  const tag = Array.isArray(term) ? term[0] : term;
  switch (tag) {
    case "article_loaded":
      return new ArticleLoaded(decode_string(term[1]), decode_string(term[2]), decode_string(term[3]), decode_list_of((t0) => decode_string(t0), term[4]));
    case "article_updated":
      return new ArticleUpdated(decode_string(term[1]));
    case "editor_errors":
      return new EditorErrors(decode_list_of((t0) => decode_string(t0), term[1]));
    default:
      throw new DecodeError("unknown push variant: " + String(tag));
  }
}


export function decode_push_editor(term) {
  const tag = Array.isArray(term) ? term[0] : term;
  switch (tag) {
    case "article_published":
      return new ArticlePublished(decode_string(term[1]));
    case "editor_errors":
      return new EditorErrors(decode_list_of((t0) => decode_string(t0), term[1]));
    default:
      throw new DecodeError("unknown push variant: " + String(tag));
  }
}


export function decode_push_home(term) {
  const tag = Array.isArray(term) ? term[0] : term;
  switch (tag) {
    case "home_data":
      return new HomeData(decode_list_of((t0) => decode_ArticlePreview_article_preview(t0), term[1]), decode_list_of((t0) => decode_string(t0), term[2]), decode_int(term[3]));
    case "article_list_updated":
      return new ArticleListUpdated(decode_list_of((t0) => decode_ArticlePreview_article_preview(t0), term[1]), decode_int(term[2]));
    default:
      throw new DecodeError("unknown push variant: " + String(tag));
  }
}


export function decode_push_login(term) {
  const tag = Array.isArray(term) ? term[0] : term;
  switch (tag) {
    case "authenticated":
      return new Authenticated(decode_string(term[1]), decode_string(term[2]));
    case "auth_error":
      return new AuthError(decode_list_of((t0) => decode_string(t0), term[1]));
    default:
      throw new DecodeError("unknown push variant: " + String(tag));
  }
}


export function decode_push_profile_username(term) {
  const tag = Array.isArray(term) ? term[0] : term;
  switch (tag) {
    case "profile_data":
      return new ProfileData(decode_Profile_profile(term[1]), decode_list_of((t0) => decode_ArticlePreview_article_preview(t0), term[2]), decode_bool(term[3]));
    case "follow_updated":
      return new FollowUpdated(decode_bool(term[1]));
    case "profile_articles":
      return new ProfileArticles(decode_list_of((t0) => decode_ArticlePreview_article_preview(t0), term[1]));
    default:
      throw new DecodeError("unknown push variant: " + String(tag));
  }
}


export function decode_push_register(term) {
  const tag = Array.isArray(term) ? term[0] : term;
  switch (tag) {
    case "registered":
      return new Registered(decode_string(term[1]), decode_string(term[2]));
    case "register_error":
      return new RegisterError(decode_list_of((t0) => decode_string(t0), term[1]));
    default:
      throw new DecodeError("unknown push variant: " + String(tag));
  }
}


export function decode_push_settings(term) {
  const tag = Array.isArray(term) ? term[0] : term;
  switch (tag) {
    case "settings_loaded":
      return new SettingsLoaded(decode_string(term[1]), decode_string(term[2]), decode_string(term[3]), decode_string(term[4]));
    case "settings_updated":
      return new SettingsUpdated(decode_string(term[1]), decode_string(term[2]));
    case "settings_error":
      return new SettingsError(decode_list_of((t0) => decode_string(t0), term[1]));
    case "logged_out":
      return new LoggedOut();
    default:
      throw new DecodeError("unknown push variant: " + String(tag));
  }
}
