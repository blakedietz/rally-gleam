//// Run `gleam format` on generated Gleam code.
//// Currently a no-op. Will shell out to `gleam format` via FFI
//// once the erlang port execution is stabilized.

pub fn format_gleam(code: String) -> String {
  code
}
